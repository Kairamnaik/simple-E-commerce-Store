# Port to run the server on (defaults to 5005)
PORT=5005

# Secret key for signing JSON Web Tokens
JWT_SECRET=super_secret_key_ecommerce_2026

# MongoDB connection string (local fallback)
MONGODB_URI=mongodb://127.0.0.1:27017/simple-ecommerce
